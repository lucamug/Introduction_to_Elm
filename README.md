# Introduction to Elm by Luca Mugnaini

To run the examples:

```
$ npm install
$ npm start
```

To run elm-live (for live browser reload):

```
node_modules/.bin/elm-live src/Main.elm --path-to-elm node_modules/.bin/elm -- --debug
```
